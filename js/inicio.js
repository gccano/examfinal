function go(){
    if (document.formingreso.usuario.value=='juana' && document.formingreso.contraseña.value=='examen')
    {document.formingreso.submit();}

    else{
        alert("Ingrese su usuario y contraseña correctos.");
    }
}

